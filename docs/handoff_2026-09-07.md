# Handoff 2026-09-07 — P9.H closed: submittable

Branch `tkde-review-revisions`, HEAD `2340b26` (NAS and WSL synced, trees clean).

## State: submittable

The IEEE TKDE manuscript passes the isolated terminal evaluation end to end.
- `make paper-final-check` EXIT=0 (strict/final gate): measured paths frozen at
  the v5-outcome submission commit `4bc1652`, no unresolved citations.
- `main.pdf` 12 pages; `supplement.pdf` 26 pages.
- Publication evidence points at the resealed formal campaign
  `formal-v113-publication-03` (contract release final-v5-contracts-v1.13).

## What closed this cycle

**Campaign reseal (formal-v113-publication-03).** The v1.11 campaign was resealed
under v1.13 after the WSL VM restarted mid-run (P9H-WSL-OUTAGE-01) and a
non-profile postgres readiness race (P9H-03-NONPROFILE-FAIL). 33/33 profile
deployments + non-profile compiler/scale segments; independent per-sample
re-verification green (9393 profile samples, 8-family split identical to v1.11;
16500+3240 non-profile; all pass). `publication-evidence-v1.json` campaign
pointer swapped (evidence sha `56e679f1…`), macros regenerated; publication-
campaign prose is macro-driven and unchanged in claims.

**Terminal-check re-green: the scale-e7 integration tail.** v1.13 added the P9.E
`final-v5-scale-e7-v1` publication to the live Catalog; several full-catalog /
full-registry consumers needed to catch up (each fixed with evidence, ledger
P9H-*):
- Frozen test expectations (activation manifest/registry 23→24, catalog 13→14
  schemas / 7→8 publications) — `scale7`/e7 legitimately joined the surface.
- Gateway loader cumulative HOT activation guard 1 GiB→2 GiB (the full Catalog's
  8 publications sum ~1.21 GiB with e7's 907 MiB; distinct from the per-profile
  `MaxHotBytesPerInstance` 1 GiB the paper cites, which is unchanged).
- The `taskgate_scale` crash-window gate test OOM'd (26 GiB) recompiling e7
  in-process; **author chose option A**: the scale-lane harness now loads the
  gate-built, digest-verified bundle (`TASKGATE_GATEWAY_PREBUILT_SNAPSHOT_DIR`),
  no second compile. test-runner mounts the snapshot-index volume :ro.
- Backfilled `config/snapshots/final-v5-scale-e7-v1.json` `expected_digests`
  (the only publication missing them; deployment-produced, catalog-verified).
- Post-restart gateway readiness wait 30s→120s (activating 1.21 GiB now exceeds
  the old script window but stays inside the 90s healthcheck).
- Supplement stray `\cite{zhang2023dprovdb}` → prose (the supplement carries no
  bibliography; comparators are cited in the main text).

## Author-only items outstanding

None blocking submission. The P9.G dataset binding is signed and installed
(successor `685a0e20…`); the campaign is sealed. No tag has been moved/pushed;
if a submission tag is wanted, that is an author decision (affects publication).

## Reproduce the terminal check

WSL `~/worktrees/agent_task_gateway`, clean tree at HEAD: `~/stage-e/post-fix-chain.sh`
(DSN suite → v5-outcome rebuild incl. compose-e2e gate). Requires a clean
worktree (the rebuild fail-closes on any dirty file beyond the untracked DSN
record — clean leftovers first). Then `make paper-final-check`. The scale-e7
compile runs once in its own 24 GiB-cgroup Compose service; the full-registry
tests read that bundle rather than recompiling.

## Next pickup

Submittable. Remaining is author-facing: final read of the built PDFs and the
submission tag decision. No self-executable P9.H items remain.

# Handoff 2026-09-07b — FactGate 改名、单匿名、GitHub 迁移完成；严格门绿

Branch `tkde-review-revisions`, HEAD `c60e85d`（WSL 与 NAS 同步，树干净）。
接续 `docs/handoff_2026-09-07.md`（P9.H closed, submittable）。**作者定：WSL 是主会话，NAS 只做 ff 同步副本。**

## 作者今日裁决（台账 FACTGATE-RENAME / -GITHUB / -NAS-SYNC / -NAS-RENAME / -FINAL-CHECK）

1. TKDE 按默认**单匿名**投稿；本稿以真名先发 arXiv 新编号；SessionBound arXiv:2607.00751 只发 v2 加取代说明。
2. 系统名 **FactGate**；标题「FactGate: A Task-Scoped Data Gateway for AI Agents with Cumulative Exposure Accounting」。
3. GitHub 组织改为通用名 **minmin-lab**；仓库 **minmin-lab/factgate**（public）；sessionbound 已归档；NAS 目录改为
   `/volume1/homes/wuminmin/github/minmin-lab/factgate`（旧路径留 symlink）。
4. GitHub 一律走 gh 认证，禁止匿名 curl（已写全局 CLAUDE.md）。

## 现状（实测）

- `make paper-final-check` 在 c60e85d rc=0；main.pdf 12 页、supplement.pdf 26 页；measured paths 与 4bc1652 无差异。
- 封存证据/契约/Go 标识符里的 `bdg`/`BDG` 不改；supplement 首段与 tab:rls-arms-supp 表题说明其为内部标签。

## 接着做这个（全部作者亲手；清单与可粘贴文本在 tmp/reports/arxiv-and-github-handover.md）

1. 填 `[AFFILIATION]`（paper/tkde/main.tex 与 supplement.tex 作者栏）。
2. arXiv：新提交本稿 → 拿到编号后 SessionBound 发 v2 → 投 TKDE 后本稿 v2 加 Submitted to IEEE TKDE；cover letter 填 `[NEW-ID]`
   （tmp/reports/p77-cover-letter-draft.md 已按 FactGate/单匿名更新）。
3. GitHub 网页：删空仓库 minmin-lab/empty-placeholder-2026-07；组织描述/显示名 Minmin Lab（token 无 admin:org）；域名 301 到
   https://github.com/minmin-lab/factgate。
4. 恢复全局 `catm-notify` 命令（今日 09:19 后从 ~/.local/bin 消失；仓库后备脚本因 mcpServers 无 catm 也不可用）。
5. 复现包位置（DataPort / Zenodo / GitHub Release）与提交 tag：作者裁决（影响发表）。

## Claude 可自做（作者填完 affiliation 后）

- 重建 PDF、核 12 页、`make paper-final-check`、合并 main+supplement 为 arXiv 上传件。

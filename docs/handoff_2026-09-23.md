# 交接 2026-09-23（P10-R2：formal-v113-publication-05 已封存登记；停在论文数字重生成与逐宏比对）

权威顺序：台账 `docs/codex_publication_execution_plan.md` 尾部 P10-R2-* 行（最新 LOOP-21）> 本文件 > `~/logs/PLAN-p10-r2-longrun.md`。
分支 `p10-r2`；`main` 仍停在 4795d50，合并要等论文能构建。

## 现在停在哪一步（2026-09-23 00:3x）

**formal campaign 完成并登记**：formal-v113-publication-05（commit faf4d09，Gateway image sha256:0541b7f8…）33 部署 + 6 份非部署执行全 pass，campaign-evidence sha256 d9db33d1…（3840 B），独立复核 VERIFY_PASS，指针 `evaluation/final-v5-wsl2/publication-evidence-v1.json` 已切到 -05，NAS 上部署逐个 MIRRORED、non-profile/campaign-evidence/两个保留 attempt 均 sha256_verified_on_nas=all。raw 66 G 不入 git（与 v113-03 惯例一致）。

**非部署段跑了三次才过**——前两次同一 warmup（outcome-merkle/100k-x10k-o0）失败，根因是 PostgreSQL 首次 autovacuum 创建 VM fork（+8192 B）落在 replay 存储快照之间，被 evaluation_outcome_v5.go:170 的精确相等检查误判（台账 LOOP-20，证据分档齐全）。作者裁决方案 2：不碰冻结路径，在仓库外的 resume 脚本里把三张 outcome 表及 TOAST 的 VACUUM 阈值拉到 INT_MAX（autoanalyze 不动），披露在 `non-profile/scale-outcome-merkle/postgres-reloptions-0{1,2,3}.txt`、publication-evidence host 字段与台账。两次失败尝试保留在 `raw/formal-v113-publication-05.nonprofile-attempt-{1,2}`（含 ATTEMPT-NOT-USED-BY-SEAL）。

## 已做到哪（2026-09-23 00:50）

evidence.tex 与 p10.tex 都已从当前证据重生成并提交（差异表 `~/logs/evidence-diff-v113-05-vs-before.txt`，台账 LOOP-22/23）；B6 吞吐宏改自 p10-b6-throughput-02；补充材料已披露非部署段调参；容器构建 rc=0，**主稿 13 页**（参考文献溢出约 1/3 页）、补充 33 页。

## 已做到哪（2026-09-23 17:30 补）

作者选 A：subphase pilot 以 warmups=1 重跑，第四次尝试 subphase-pilot-17 通过、登记、镜像（台账 LOOP-25；三次失败尝试保留在 raw/subphase-pilot-1{4,5,6}.* 并写明原因）。S1/SF1 回到 warm 量级（Go 侧 57%），主稿「Go 侧主导」句成立。补充材料的 pilot 来源引用已改为实际 run（-17/-13、provsql-leaf-05）。GPT 评审稿在 main（5e9e531）。

## 接着做这个

1. **等作者一项裁决**（台账 LOOP-23）：主稿回 12 页删哪 1/3 页（候选：47a467d/ffd6981/d0c1d6a 新增句精简、参考文献合并；不删证据表）。
2. GPT 评审意见回来后按 `docs/agents/paper-review-loop.md` 处理（text-only 直接改；needs-new-evidence 上报）。
3. 每轮改完：build-container.sh → 12 页 → `make paper-final-check` rc=0（要求树净且 generated/evidence.tex 与 HEAD 一致）→ 推 p10-r2 与 main。
3. 技术欠账（不阻塞发表）：(a) outcomeRadixStorageSnapshot 只计 main fork——internal/ 冻结路径，等 v5-outcome 链重建时做；(b) nas-mirror-watch.sh 的 pass() 不镜像 non-profile/，补上；(c) diag worktree `/home/wmm/worktrees/factgate-diag` 含未提交诊断补丁与 zz_diag_order_test.go，可删；(d) test_final_v5_pilot_evidence 三个用例复制已丢失的 subphase-pilot-06 raw，改用现存 pilot。
4. 合并 p10-r2 → main（论文能构建后）。
5. B5 的 (iv-ledger) 同输入对照仍未做（docs/p10_r2_b5_ablation_design.md），论文已如实写明限制；有余力再补。

## 等作者的事

- 论文逐宏差异表出来后的取舍。
- 591MB 原始样本存缴 + DOI（README Artifact 小节那一行；主稿指向它）。

## 环境要点

同 handoff_2026-09-20.md「环境要点」。补充：campaign 期间（含非部署段 resume）台账也不能写——resume 守卫要求 HEAD==发射提交且树净，待登记条目先记 `~/logs/pending-registration.md`，封存后再入台账。

# 交接 2026-09-20（P10-R2：评审整改 + 丢失证据重跑；停在等作者压缩 WSL VHD）

权威顺序：台账 `docs/codex_publication_execution_plan.md` 尾部 P10-R2-* 行 > 本文件 > `~/logs/PLAN-p10-r2-longrun.md`（仓库外的执行细节与脚本位置）。
分支 `p10-r2`（origin 同步）；`main` 仍停在 4795d50，合并要等论文能构建。

## 现在停在哪一步

**等作者压缩 WSL VHD。** 停点条件已全部满足【实跑核过】：无 campaign 在跑、证据全部 NAS 核验（各 campaign 的 MIRROR_DONE + FINAL_VERIFY 0 不符）、工作树净且已推 origin。
作者压缩完重启后，按下面「接着做这个」继续。

压缩命令（Windows 管理员 PowerShell）：`wsl --shutdown` → `Optimize-VHD -Path "D:\WSL\Ubuntu-22.04\ext4.vhdx" -Mode Full`（无 Hyper-V 则 diskpart：select vdisk file=… / attach vdisk readonly / compact vdisk / detach vdisk）；建议同时 `wsl --manage Ubuntu-22.04 --set-sparse true`。
理由：ext4.vhdx 521G，内部仅用约 115G，约 380G 卡在镜像里；正式 campaign 33 部署需要空间。

## 今天完成（全部已推 origin/p10-r2）

1. **六个丢失 pilot 重跑完毕，全部 rc=0**：pilot-adversary-05(9 部署)、pilot-counter-rigor-03(12)、subphase-pilot-12(2)、pilot-benign-07(9)、pilot-footprint-08(2)、pilot-scale7-05(3)。每个都逐部署镜像到 NAS 并在 NAS 副本上核 sha256（197/257/57/197/57/98 文件，0 不符）。
2. **登记**：pilot-evidence-v1.json 新增 7 条（scale7 campaign 登记为 pilot-scale7-05 与 pilot-compare7-02 两面，因该剖面同时产两族样本），旧 7 条加 superseded_by 并注明「raw 随 2026-09-19 WSL 重建丢失」。
3. **复现性实证**：compare7 轨迹与丢失运行所出宏逐值相同（37 接受=16 唯一+21 重复、23 拒、首拒第 23 步、repeat_charges=0）。
4. **binding（作者裁决 A）**：重生成件与作者会话 #3 已批准件逐字节相同（685a0e20…/110584B，--validate-binding valid），沿用 #3 批准，不签新会话；安装于 `~/.taskgate-signing-backup-p9g`（0700/0600）；决策入 docs/final_v5_author_decisions.md。
5. **D 盘归档删除（作者 option 2）**：两侧 4218 个文件 sha256 逐行比对全同 → p10-b2-agent-01 复制进 WSL（符号链接改真实目录，三方摘要一致）→ 10 个目录逐个 rd /s /q 全 rc=0 → D 盘 673G → 1.2T 可用（约 497G 回收）。NAS 副本长期保留，是这些实验仅存原始数据。
6. **磁盘卫生**：每个 campaign 在 MIRROR_DONE 后删除 snapshot-index-artifacts-full / profile-artifacts（可由源码重建、不进 NAS 镜像）；WSL 根现 842G 可用。

## 接着做这个（作者压缩并重启后）

1. **重跑仍缺的 pilot（要用 binding，串行，约 7–9 小时）**：artifact-3dep-1/2/3（run-artifact-targeted.sh，需 ATTESTATION_QUALIFICATION/POSTGRESQL_IDENTITY，git 内有 v111-01 那套）、subphase-pilot-08 的替代（result-heavy 剖面，补 S6/100k-x16 格）、provsql-leaf-pilot-04 的替代、baseline-s1-s2 的替代（run-baseline-targeted.sh，不需 binding）、p10-b6-throughput-02（launch-throughput-retry-pilot.sh 设 THROUGHPUT_CLIENT_RETRIES=0、去掉 control pass）。发射一律用 launch-profile-pilot.sh 那套前置自检 + NAS 镜像看守。
2. **formal v113 重跑**（新 id formal-v113-publication-04，33 部署 + 非部署段，约 15 小时）：先写 publication 类发射脚本（见 PLAN §2.C，含全部前置与 per-deployment 剪枝，避免 726G 峰值）、查台账 P9H-OOM-RESCOPE 的 scale 段内存问题、用 result-heavy 单剖面演练一次；**发射前必须再次知会作者并等确认**。
3. **论文**：全部 raw 到位后 generate_p10_evidence.py → build-container.sh → 12 页 0 溢出 → make paper-final-check rc=0 → **逐宏列出与旧 evidence.tex 的差异交作者** → 按差异改正文 → 合并 p10-r2 到 main。
4. B5 的 (iv-ledger) 同输入账本对照仍未做（设计在 docs/p10_r2_b5_ablation_design.md），论文已如实写明限制；有余力再补。

## 等作者的事

- **压缩 WSL VHD**（现在）。
- **591MB 原始样本存缴 + DOI**（README Artifact 小节那一行；主稿指向它）。
- **formal campaign 发射确认**；以及内存是否要加到 ≥48GiB（scale 段 e7 编译峰值约 26GiB／本机 29GiB）。

## 环境要点

Go 在 ~/.local/go（PATH 加 ~/.local/bin）；psql 是 docker shim；无 gh、无 GitHub 凭证 → push 走 `git push nas` 再由 NAS `git push origin`，之后必须 `git fetch origin`（正式构建要求 HEAD==origin/分支）；MinIO 镜像从 quay 拉后本地打 tag；.env 本地生成（不入库）；NAS 证据共享 /mnt/ctg-pto-dev（CIFS hard,0600/0700）；日志与计划在 ~/logs/。
铁律：campaign 运行期间不改任何被跟踪文件；证据每部署即镜像 NAS 并核 sha256；说「入库」必附 git ls-files 读数；签署与批准文本永不代签。

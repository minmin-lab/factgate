# 交接 2026-09-19（P10-R2：GPT 第二轮评审整改，中途）

分支 `p10-r2`（origin 已同步）；`main` 停在 4795d50（仅文档）。台账 `docs/codex_publication_execution_plan.md` 尾部 P10-R2-* 行是权威；清单 `docs/p10_r2_review_response_plan.md`。

## 需要作者的事（按阻塞面排序）

1. **论文在本机无法构建**：重建后的 WSL 没有 `pilot-evidence-v1.json` 登记的 17 个 pilot 原始目录，也没有 `p10-b6-throughput-01`；NAS、`D:\wsl-data`、`D:\SynologyDrive` 都找过，没有。`generate_evidence.py`（冻结 measured path）经 `final_v5_pilot_evidence.py:117-119` 硬性要求它们在场，`make paper-final-check` 因此不可执行。要么找到旧 VHD/备份恢复这 18 个目录到 `evaluation/final-v5-wsl2/raw/`，要么作者裁决放宽校验（改 measured path），要么重跑对应 pilot（代价最大）。**所有 C/D 组文本改动因此都停在「已改未构建」。**
2. **591 MB 原始样本的 DOI 存缴**（D4）：主稿已指向 GitHub 仓库 + README 的 DOI 行，DOI 行标「待作者存缴」。
3. **签署的 Dataset Binding**：只有 Scale/Artifact/ProvSQL 类 cell 需要；C4b（benign-x4）与 B5 端到端（Baseline 格）都不需要，已用占位文件。TPC-H SF1/SF10 数据随旧 WSL 丢失，analytics-orders 两格暂不做。

## 已完成（分支 p10-r2）

- C1 预算扫描：单条精确重放曲线，撤回上下界；数值与旧 corpus_model 逐点一致；README 记撤回理由与反例。
- C2 时序：主文改为条件模型估算；补充材料加三条限制。
- C3 代理：原始记录逐步核实 Outcome 计费 [1,2,2,2,2,2]=11/12，第 7 条需 2 不可容纳；登记件 note 改正并注明原措辞与根因；`correct` 定义写进表 caption。
- C4a 吞吐：表加 requested/CONFLICT 列，账本一致性与请求完成率分述；主稿补 80/600。
- D1/D2/D4：55% 前置、代理实验定性、artifact URL。
- C4b：设计冻结、adapter `-throughput-client-retries`、hook、发射脚本、分析器全部就绪；未发射（等 CPU 空闲，本机同时只能跑一个 campaign 级任务）。
- E0/E1（B5）：设计冻结；朴素精确集合账本 + 良性轨迹本机实测（21/23 与 sweep 一致，整条轨迹 37–39 s，7–12 µs/事实，1.23M 行 560–620 MB）；`-b5-ablation` 驱动、无记账 catalog 孪生（生产加载器校验通过）、`run-b5-ablation.sh`、分析器就绪；未跑。

## 环境（重建 WSL 后）

见 `docs/agents/environment.md` 末节：Go 在 `~/.local/go`；无 gh，push 走 `git push nas` → NAS 上 `git push origin`；MinIO 镜像从 quay 拉后本地打 tag；`.env` 已按文档本地生成（不入库）；psql 是 docker shim；本地 docker `b5-pg`（127.0.0.1:55432）供 naiveledger 测试。

## 接着做这个

1. 开发栈或 fresh 部署的 scale-e7 索引编译结束后：`ARM=iv B5_SAMPLES=1 evaluation/final-v5-wsl2/scripts/run-b5-ablation.sh` smoke，再 `ARM=i`；通了就 10 样本×3 部署。
2. 然后 `evaluation/final-v5-wsl2/scripts/launch-throughput-retry-pilot.sh`（C4b，约 1.5 h）。
3. 原始目录恢复后：容器构建 → 12 页 → `make paper-final-check` → 把 p10-r2 合到 main。

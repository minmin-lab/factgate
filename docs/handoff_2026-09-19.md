# Handoff 2026-09-19 — P10 审稿回应清单收口：A 全部、B1–B4/B6–B8 完成；余项均待作者

接续 `docs/handoff_2026-09-18.md`。Branch `tkde-review-revisions`（WSL 主会话；NAS 只做 ff 同步；`main` 已折叠到同一提交）。

## 现状（实测，2026-09-19 03:30 UTC+8）

- 最新门禁：`make paper-final-check` rc=0 于 **07fa10c**；主稿 **12 页**、补充 **31 页**。之后只有台账提交（fb7b3b0 起）。
- 清单 `docs/p10_review_response_plan.md`：A1–A16 全部 DONE（A3 复现包位置待作者）；B1、B2、B3、B4、B6、B7、B8 DONE；**B5 待作者裁决**（公共路径消融需要 `internal/` 运行时开关，属冻结 measured path，改即门禁红）。
- 本轮新增证据（全部 pilot 类、publication_eligible false；宏经 `paper/tkde/generate_p10_evidence.py`，与冻结的 generate_evidence.py 分离）：
  - B2 `p10-b2-agent-01`（36 次无头 Claude agent 运行）与 B6 `p10-b6-throughput-01`（72 轮吞吐）登记在**新建的** `evaluation/final-v5-wsl2/p10-pilot-evidence-v1.json`（不能登旧 pilot-evidence-v1.json：冻结生成器按旧布局校验每一项，登入即构建失败）。
  - B4 `evaluation/counter-aligned/`、B7 `evaluation/timing-channel/`、B8 `evaluation/passant-comparison/`（分析类，digest 绑定）。
- arXiv v2 上传件已按 07fa10c 刷新：`tmp/reports/arxiv-v2/factgate-arxiv-2607.00751v2.pdf`（43 页 = 12 + 31）、`abstract.txt`（未变，196–198 词按计法）；同名文件已 scp 到 NAS `~/`。
- 磁盘：本轮曾满盘（98%）导致 B6 冒烟失败；已把已被 v113 取代的封存/试跑目录移到 `/mnt/d/wsl-data/taskgate-evidence/archived-campaigns/`（移动不删除）。**`~/stage-e/archive-p10-retry.sh` 仍在后台**搬 formal-v111-publication-03 剩余 176G（日志 `~/stage-e/logs/archive-p10.log`，看到 `RETRY_DONE` 即完成）；主树 raw/ 现约 200G，free ≈ 250G。
- 环境残留（可留可删）：用户级 Rust 工具链 `~/.cargo`（rustup minimal，不改 shell profile）与 Passant 克隆 `~/passant`（含 .venv）——B8 复现需要；B8 用的独立 Postgres 容器 `passant-pg` 已删。`~/wt-pilot`（subphase-pilot）干净，= 0bd1a95。

## 接着做这个（作者亲手；文本见 tmp/reports/arxiv-and-github-handover.md 与 p77-cover-letter-draft.md）

1. **arXiv Replace 2607.00751 → v2**：新标题、`abstract.txt`、合并 PDF、Comments 文本（B1）。
2. **投 TKDE**（cover letter 已就绪，Preprint disclosure 段在）；投后发 v3 加 Submitted（B2）。
3. **A3 复现包位置**：主稿 §8.3 仍是 `[artifact DOI/URL]` 占位；定位置后由 Claude 填并重跑门禁。
4. **B5 裁决**：是否解冻 measured path 做公共路径消融（四层：不记账 / 推导不限预算 / 朴素集合账本 / 完整）——若做，需新 submission commit 与三部署重跑；不做则在回应信里按 A11 论证。
5. GitHub 网页：删 `minmin-lab/empty-placeholder-2026-07`；组织描述/显示名；域名 301。
6. `describe_data_product` 是否公告 `to_char`/`date_trunc`/`/`（measured path）——B1 held-out 里 6 条「需新规则」与此相关。

## 下一会话 Claude 开工顺序

1. 体检先 `df -h /` 与 `du -sh evaluation/final-v5-wsl2/raw/* | sort -rh | head`；低于 100G 先归档（作者规则，见 memory auto-archive-evidence-to-d；本轮教训：会话开工未装归档看守）。
2. 若 `archive-p10.log` 无 `RETRY_DONE`，重跑 `~/stage-e/archive-p10-retry.sh`（幂等，rsync --remove-source-files --inplace）。
3. 作者给出 1–6 中任一项的裁决后：改 → 构建 → 12 页 → `make paper-final-check` → push 分支与 HEAD:main → NAS ff → 台账。
